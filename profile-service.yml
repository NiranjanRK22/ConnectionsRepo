spring:
  datasource:
    url: jdbc:mysql://localhost:3306/connections
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5Dialect